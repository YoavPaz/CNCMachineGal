#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include "main.h"

class functions{
    public:
    functions() {};
    static void G1Move(ParamValue* params, int count);
    static void G0Move(ParamValue* params, int count);

};

#endif
